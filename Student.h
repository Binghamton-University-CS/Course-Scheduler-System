#include <string>

class Student {
  public:
    Student(string BNumber, string userID, string first, string last);
  private:
    string BNumber;
    string userID;
    string first;
    string last;  
};
