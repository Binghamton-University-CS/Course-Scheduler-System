#include <string>
#include "Student.h"

Student::Student(string first, string last, string userID, string bNumber) {
  this->first = first;
  this->last = last;
  this->userID = userID;
  this->bNumber = bNumber;
}
